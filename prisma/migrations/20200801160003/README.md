# Migration `20200801160003`

This migration has been generated by David S. García (Azæl) at 8/1/2020, 4:00:03 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "OrderType" AS ENUM ('MANTEINANCE', 'INSTALLATION', 'WARRANTY');

CREATE TYPE "OrderStatus" AS ENUM ('CREATED', 'ASSIGNATED', 'IN_PROGRESS', 'FINISHED');

CREATE TABLE "public"."Client" (
"id" text  NOT NULL ,
"document" integer  NOT NULL ,
"name" text  NOT NULL ,
"lastName" text  NOT NULL ,
"email" text  NOT NULL ,
"password" text  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Technician" (
"id" text  NOT NULL ,
"document" integer  NOT NULL ,
"name" text  NOT NULL ,
"lastName" text  NOT NULL ,
"email" text  NOT NULL ,
"password" text  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Order" (
"id" text  NOT NULL ,
"date" timestamp(3)  NOT NULL ,
"updatedAt" timestamp(3)  NOT NULL ,
"clientId" text  NOT NULL ,
"technicianId" text  NOT NULL ,
"rating" integer   ,
"ratingExtra" text   ,
"status" "OrderStatus" NOT NULL ,
"type" "OrderType" NOT NULL ,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "Client.email" ON "public"."Client"("email")

CREATE UNIQUE INDEX "Technician.email" ON "public"."Technician"("email")

ALTER TABLE "public"."Order" ADD FOREIGN KEY ("clientId")REFERENCES "public"."Client"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Order" ADD FOREIGN KEY ("technicianId")REFERENCES "public"."Technician"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200801160003
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,57 @@
+// Service Kiosk's Prisma Schema
+
+datasource db {
+  provider = "postgres"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Client {
+  id String @id @default(cuid())
+  document Int
+  name String
+  lastName String
+  email String @unique
+  password String
+  placedOrders Order[]
+}
+
+model Technician {
+  id String @id @default(cuid())
+  document Int
+  name String
+  lastName String
+  email String @unique
+  password String
+  assignedOrders Order[]
+}
+
+enum OrderType {
+  MANTEINANCE
+  INSTALLATION
+  WARRANTY
+}
+
+enum OrderStatus {
+  CREATED
+  ASSIGNATED
+  IN_PROGRESS
+  FINISHED
+}
+
+model Order {
+  id String @id @default(cuid())
+  date DateTime
+  updatedAt DateTime @updatedAt
+  clientId String
+  client Client @relation(fields: [clientId], references: [id])
+  technicianId String
+  technician Technician @relation(fields: [technicianId], references: [id])
+  rating Int?
+  ratingExtra String?
+  status OrderStatus
+  type OrderType
+}
```



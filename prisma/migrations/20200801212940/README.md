# Migration `20200801212940`

This migration has been generated by David S. García (Azæl) at 8/1/2020, 9:29:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "UserRole" AS ENUM ('CLIENT', 'TECHNICIAN');

CREATE TABLE "public"."User" (
"id" SERIAL,
"document" integer  NOT NULL ,
"firstName" text  NOT NULL ,
"lastName" text  NOT NULL ,
"email" text  NOT NULL ,
"password" text  NOT NULL ,
"role" "UserRole" NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Ticket" (
"id" text  NOT NULL ,
"date" timestamp(3)  NOT NULL ,
"updatedAt" timestamp(3)  NOT NULL ,
"clientId" integer  NOT NULL ,
"technicianId" integer  NOT NULL ,
"rating" integer   ,
"ratingExtra" text   ,
"status" "TicketStatus" NOT NULL ,
"type" "TicketType" NOT NULL ,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Ticket" ADD FOREIGN KEY ("clientId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Ticket" ADD FOREIGN KEY ("technicianId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200801212940
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,53 @@
+// Service Kiosk's Prisma Schema
+
+datasource db {
+  provider = "postgres"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+enum UserRole {
+  CLIENT
+  TECHNICIAN
+}
+
+model User {
+  id Int @id @default(autoincrement())
+  document Int
+  firstName String
+  lastName String
+  email String @unique
+  password String
+  clientTickets Ticket[] @relation(name: "TicketClient")
+  technicianTickets Ticket[] @relation(name: "TicketTechnician")
+  role UserRole
+}
+
+enum TicketType {
+  MAINTENANCE
+  INSTALLATION
+  WARRANTY
+}
+
+enum TicketStatus {
+  ASSIGNED
+  IN_PROGRESS
+  FINISHED
+}
+
+model Ticket {
+  id String @id @default(cuid())
+  date DateTime
+  updatedAt DateTime @updatedAt
+  clientId Int
+  technicianId Int
+  client User @relation(name: "TicketClient", fields: [clientId], references: [id])
+  technician User @relation(name: "TicketTechnician", fields: [technicianId], references: [id])
+  rating Int?
+  ratingExtra String?
+  status TicketStatus
+  type TicketType
+}
```


